(function() {
    'use strict';

    angular.module('app')
        .controller('clockWidgetController', ['$timeout', '$scope',
            function($timeout, $scope) {

            }
        ]);
})();